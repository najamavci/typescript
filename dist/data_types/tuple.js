"use strict";
//Tuple types allow you to express an array with a fixed number of elements whose types are known, but need not be the same.
let x;
x = ["hello", 10]; //the correct way of initializing tuple
//x = [10, "hello"]; //   Incorrect way of initializing tuple
console.log(x);
let f;
